import { assert, describe, it } from "vitest";
import { solve25a, solve25b } from "./day25";

describe("day25", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve25a", () => {
    const sampleData = [0];
    assert.equal(solve25a(sampleData), 0);
  });
  it("solve25b", () => {
    const sampleData = [0];
    assert.equal(solve25b(sampleData), 0);
  });
});
