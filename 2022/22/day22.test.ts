import { assert, describe, it } from "vitest";
import { solve22a, solve22b } from "./day22";

describe("day22", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve22a", () => {
    const sampleData = [0];
    assert.equal(solve22a(sampleData), 0);
  });
  it("solve22b", () => {
    const sampleData = [0];
    assert.equal(solve22b(sampleData), 0);
  });
});
