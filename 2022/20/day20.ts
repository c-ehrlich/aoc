import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve20a(input: number[]) {
  return 0;
}

export function solve20b(input: number[]) {
  return 0;
}

console.log(solve20a(inputA));
console.log(solve20b(inputB));
