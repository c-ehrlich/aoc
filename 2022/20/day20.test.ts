import { assert, describe, it } from "vitest";
import { solve20a, solve20b } from "./day20";

describe("day20", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve20a", () => {
    const sampleData = [0];
    assert.equal(solve20a(sampleData), 0);
  });
  it("solve20b", () => {
    const sampleData = [0];
    assert.equal(solve20b(sampleData), 0);
  });
});
