import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve17a(input: number[]) {
  return 0;
}

export function solve17b(input: number[]) {
  return 0;
}

console.log(solve17a(inputA));
console.log(solve17b(inputB));
