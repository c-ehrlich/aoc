import { assert, describe, it } from "vitest";
import { solve17a, solve17b } from "./day17";

describe("day17", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve17a", () => {
    const sampleData = [0];
    assert.equal(solve17a(sampleData), 0);
  });
  it("solve17b", () => {
    const sampleData = [0];
    assert.equal(solve17b(sampleData), 0);
  });
});
