import { assert, describe, it } from "vitest";
import { solve13a, solve13b } from "./day13";

describe("day13", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve13a", () => {
    const sampleData = [0];
    assert.equal(solve13a(sampleData), 0);
  });
  it("solve13b", () => {
    const sampleData = [0];
    assert.equal(solve13b(sampleData), 0);
  });
});
