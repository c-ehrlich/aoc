import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve13a(input: number[]) {
  return 0;
}

export function solve13b(input: number[]) {
  return 0;
}

console.log(solve13a(inputA));
console.log(solve13b(inputB));
