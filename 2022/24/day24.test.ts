import { assert, describe, it } from "vitest";
import { solve24a, solve24b } from "./day24";

describe("day24", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve24a", () => {
    const sampleData = [0];
    assert.equal(solve24a(sampleData), 0);
  });
  it("solve24b", () => {
    const sampleData = [0];
    assert.equal(solve24b(sampleData), 0);
  });
});
