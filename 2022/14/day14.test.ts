import { assert, describe, it } from "vitest";
import { solve14a, solve14b } from "./day14";

describe("day14", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve14a", () => {
    const sampleData = [0];
    assert.equal(solve14a(sampleData), 0);
  });
  it("solve14b", () => {
    const sampleData = [0];
    assert.equal(solve14b(sampleData), 0);
  });
});
