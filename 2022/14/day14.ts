import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve14a(input: number[]) {
  return 0;
}

export function solve14b(input: number[]) {
  return 0;
}

console.log(solve14a(inputA));
console.log(solve14b(inputB));
