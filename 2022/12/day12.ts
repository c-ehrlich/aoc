import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve12a(input: number[]) {
  return 0;
}

export function solve12b(input: number[]) {
  return 0;
}

console.log(solve12a(inputA));
console.log(solve12b(inputB));
