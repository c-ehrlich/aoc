import { assert, describe, it } from "vitest";
import { solve21a, solve21b } from "./day21";

describe("day21", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve21a", () => {
    const sampleData = [0];
    assert.equal(solve21a(sampleData), 0);
  });
  it("solve21b", () => {
    const sampleData = [0];
    assert.equal(solve21b(sampleData), 0);
  });
});
