import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve21a(input: number[]) {
  return 0;
}

export function solve21b(input: number[]) {
  return 0;
}

console.log(solve21a(inputA));
console.log(solve21b(inputB));
