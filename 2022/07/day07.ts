import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve07a(input: number[]) {
  return 0;
}

export function solve07b(input: number[]) {
  return 0;
}

console.log(solve07a(inputA));
console.log(solve07b(inputB));
