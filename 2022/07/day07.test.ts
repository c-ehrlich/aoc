import { assert, describe, it } from "vitest";
import { solve07a, solve07b } from "./day07";

describe("day07", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve07a", () => {
    const sampleData = [0];
    assert.equal(solve07a(sampleData), 0);
  });
  it("solve07b", () => {
    const sampleData = [0];
    assert.equal(solve07b(sampleData), 0);
  });
});
