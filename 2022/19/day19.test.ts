import { assert, describe, it } from "vitest";
import { solve19a, solve19b } from "./day19";

describe("day19", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve19a", () => {
    const sampleData = [0];
    assert.equal(solve19a(sampleData), 0);
  });
  it("solve19b", () => {
    const sampleData = [0];
    assert.equal(solve19b(sampleData), 0);
  });
});
