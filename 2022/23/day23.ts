import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve23a(input: number[]) {
  return 0;
}

export function solve23b(input: number[]) {
  return 0;
}

console.log(solve23a(inputA));
console.log(solve23b(inputB));
