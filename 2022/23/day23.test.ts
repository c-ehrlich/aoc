import { assert, describe, it } from "vitest";
import { solve23a, solve23b } from "./day23";

describe("day23", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve23a", () => {
    const sampleData = [0];
    assert.equal(solve23a(sampleData), 0);
  });
  it("solve23b", () => {
    const sampleData = [0];
    assert.equal(solve23b(sampleData), 0);
  });
});
