import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve16a(input: number[]) {
  return 0;
}

export function solve16b(input: number[]) {
  return 0;
}

console.log(solve16a(inputA));
console.log(solve16b(inputB));
