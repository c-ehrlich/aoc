import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve04a(input: number[]) {
  return 0;
}

export function solve04b(input: number[]) {
  return 0;
}

console.log(solve04a(inputA));
console.log(solve04b(inputB));
