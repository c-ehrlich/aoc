import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve09a(input: number[]) {
  return 0;
}

export function solve09b(input: number[]) {
  return 0;
}

console.log(solve09a(inputA));
console.log(solve09b(inputB));
