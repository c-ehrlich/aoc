import { assert, describe, it } from "vitest";
import { solve15a, solve15b } from "./day15";

describe("day15", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve15a", () => {
    const sampleData = [0];
    assert.equal(solve15a(sampleData), 0);
  });
  it("solve15b", () => {
    const sampleData = [0];
    assert.equal(solve15b(sampleData), 0);
  });
});
