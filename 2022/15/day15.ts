import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve15a(input: number[]) {
  return 0;
}

export function solve15b(input: number[]) {
  return 0;
}

console.log(solve15a(inputA));
console.log(solve15b(inputB));
