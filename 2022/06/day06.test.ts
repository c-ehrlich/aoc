import { assert, describe, it } from "vitest";
import { solve06a, solve06b } from "./day06";

describe("day06", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve06a", () => {
    const sampleData = [0];
    assert.equal(solve06a(sampleData), 0);
  });
  it("solve06b", () => {
    const sampleData = [0];
    assert.equal(solve06b(sampleData), 0);
  });
});
