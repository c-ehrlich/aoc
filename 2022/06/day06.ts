import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve06a(input: number[]) {
  return 0;
}

export function solve06b(input: number[]) {
  return 0;
}

console.log(solve06a(inputA));
console.log(solve06b(inputB));
