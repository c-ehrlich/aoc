import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve05a(input: number[]) {
  return 0;
}

export function solve05b(input: number[]) {
  return 0;
}

console.log(solve05a(inputA));
console.log(solve05b(inputB));
