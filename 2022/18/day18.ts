import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve18a(input: number[]) {
  return 0;
}

export function solve18b(input: number[]) {
  return 0;
}

console.log(solve18a(inputA));
console.log(solve18b(inputB));
