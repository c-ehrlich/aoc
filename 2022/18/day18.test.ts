import { assert, describe, it } from "vitest";
import { solve18a, solve18b } from "./day18";

describe("day18", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve18a", () => {
    const sampleData = [0];
    assert.equal(solve18a(sampleData), 0);
  });
  it("solve18b", () => {
    const sampleData = [0];
    assert.equal(solve18b(sampleData), 0);
  });
});
