import { assert, describe, it } from "vitest";
import { solve11a, solve11b } from "./day11";

describe("day11", () => {
  it("input", () => {
    const sampleData = [0];
    assert.deepEqual(sampleData, [0]);
  });
  it("solve11a", () => {
    const sampleData = [0];
    assert.equal(solve11a(sampleData), 0);
  });
  it("solve11b", () => {
    const sampleData = [0];
    assert.equal(solve11b(sampleData), 0);
  });
});
