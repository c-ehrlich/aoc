import fs from "fs";

const inputA = [0];
const inputB = [0];

export function solve11a(input: number[]) {
  return 0;
}

export function solve11b(input: number[]) {
  return 0;
}

console.log(solve11a(inputA));
console.log(solve11b(inputB));
